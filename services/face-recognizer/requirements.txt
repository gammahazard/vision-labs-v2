# services/face-recognizer/requirements.txt
#
# Dependencies for the face recognition service.
# InsightFace generates 512-dim face embeddings, ONNX Runtime runs on GPU.
#
# IMPORTANT: numpy must be <2 — onnxruntime/insightface are compiled against NumPy 1.x

# NumPy — MUST be pinned <2 (onnxruntime + insightface compiled against 1.x ABI)
numpy>=1.24.0,<2

# Face recognition model
insightface==0.7.3

# ONNX Runtime with CUDA support (GPU inference) — 1.20+ supports CUDA 12.x including 12.8
onnxruntime-gpu>=1.20.0,<1.23

# Redis client
redis==5.2.1
async_timeout>=4.0.0

# FastAPI for enrollment REST API
fastapi==0.115.0
uvicorn[standard]==0.32.0

# OpenCV for face crop extraction
opencv-python-headless==4.10.0.84

# Required by insightface
Cython>=0.29.0
