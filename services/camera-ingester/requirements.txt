# services/camera-ingester/requirements.txt
#
# Dependencies for the camera ingester service.
# Keep this minimal — the ingester only reads RTSP and publishes to Redis.

# OpenCV headless — no GUI needed in a Docker container.
# We only use it for RTSP capture and JPEG encoding.
opencv-python-headless==4.13.0.92

# Redis client for publishing frames to Redis Streams.
redis==5.3.1
